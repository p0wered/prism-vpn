export function LandingPage() {
  return (
    <main>
      {/* Этап 2: Hero (100vh, WebGL-фон, grain) */}
      <section className="flex min-h-screen flex-col items-center justify-center gap-4 px-6 text-center">
        <h1 className="text-5xl font-semibold tracking-tight">PrismVPN</h1>
        <p className="max-w-md text-fg-muted">
          Fast, private VPN with gaming servers. Landing is under construction.
        </p>
      </section>
      {/* Этап 3: Features (bento) → Pricing → FAQ → Footer */}
    </main>
  )
}
